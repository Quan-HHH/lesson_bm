const defaultState = {
    category: "",
    alpha: "",
    singerList: [],
    enterLoading: true,
    listOffset: 0
}

export default (state = defaultState, action) => {
    switch (action.type) {
        // case 
        default:
            return state;
    }
}